# vps-setup
Debian/Ubuntu Initialization

## 功能特点
- 主机名交互配置
- Swap 自动配置
- ssh 端口和密码配置
- Fail2ban 自动配置

## 一键脚本
```
apt install curl -y && bash <(curl -fsSL https://raw.githubusercontent.com/Vegas7/vps-setup/main/install.sh)
```
